console.log("hello");

/**
 * Built in modules are the modules that the nodes ships with
 * They are also reffered to as core modules
 * you have to import them before you can use them 
 * const path = require("node:path"); prefix them with to signify that its a built in module
 * The main core modules are
    1. path module
    2. events module
    3. fs (file system) module
    4. http
 */
